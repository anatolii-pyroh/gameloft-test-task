import { CustomThemeConfig } from "tailwindcss/types/config";

const container: CustomThemeConfig["container"] = {
  center: true,
  padding: "1rem",
  screens: {
    "2xl": "100%",
  },
};

export default container;
